# Copiar como .env y rellenar. NUNCA commitear el .env real.

# --- Neon (PostgreSQL) : UNA BASE DE DATOS POR MICROSERVICIO ---
DB_HOST=ep-wispy-bar-ap1s32ne-pooler.c-7.us-east-1.aws.neon.tech
DB_USERNAME=neondb_owner
DB_PASSWORD=CAMBIAR_POR_LA_REAL

# cada microservicio tiene su propia base fisica, no un schema compartido
DB_NAME_AUTH=auth_db
DB_NAME_VENTAS=ventas_db
DB_NAME_INVENTARIO=inventario_db

# --- JWT : el MISMO en ms-auth y api-gateway ---
JWT_SECRET=CAMBIAR_POR_EL_MISMO_DE_run-ms-auth.ps1

# --- MySQL (ms-rrhh) : su propia base, en el contenedor ---
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=rrhh
MYSQL_USER=rrhh_user
MYSQL_PASSWORD=rrhh_pass
